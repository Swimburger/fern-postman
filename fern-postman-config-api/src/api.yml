# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

types:
  PostmanCustomConfig:
    properties:
      publish: PostmanPublishConfig
      variables: map<string, string>

  PostmanPublishConfig:
    properties:
      token: string
      workspaceId: optional<string>
